import XCTest
@testable import SwiftNFCClosureHandler

final class SwiftNFCClosureHandlerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftNFCClosureHandler().text, "Hello, World!")
    }
}
